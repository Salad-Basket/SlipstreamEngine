<?xml version="1.0" encoding="utf-8" ?>
<InputManager>
  <Methods>
    <SetWindow>
      <summary> Sets the window variable in InputManager to <paramref name="window" /> </summary>
      <param name="window">The window to set to</param>
    </SetWindow>
    <AddAction>
      <summary> Adds an <paramref name="action" /> to invoke on KeyDown </summary>
      <param name="action">The action to be added</param>
    </AddAction>
    <RemoveAction>
      <summary> Removes an action at an <paramref name="index" /> to invoke on KeyDown </summary>
      <param name="index">The index at which to remove the action from the list</param>
    </RemoveAction>
    <SwitchAction>
      <summary>
        Removes the action at the <paramref name="index" /> and adds the <paramref name="action" />
      </summary>
      <param name="index">The index at which to remove the action from the list</param>
      <param name="action">The action to be added</param>
    </SwitchAction>
    <CheckKeys>
      <summary> Changes a <paramref name="key" /> into an integer </summary>
      <param name="key">The key to change into an integer</param>
    </CheckKeys>
  </Methods>
  
  <Variables>
    <window>
      <summary> The reference to the window the InputManager </summary>
    </window>
    <actions>
      <summary> The list of actions invoked when KeyDown </summary>
    </actions>
    <key>
      <summary> The key that was pressed </summary>
    </key>
    <GetInstance>
      <summary> Get the singleton reference of InputManager </summary>
    </GetInstance>
  </Variables>
</InputManager>
